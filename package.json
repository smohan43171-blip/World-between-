{
  "name": "world-between-us-backend",
  "version": "2.0.0",
  "description": "Production backend for World Between Us",
  "main": "server.js",
  "scripts": {
    "start":   "node server.js",
    "dev":     "nodemon server.js",
    "test:db": "node -e \"require('./db/database').init().then(() => { console.log('DB OK'); process.exit(0); })\""
  },
  "dependencies": {
    "bcryptjs":      "^2.4.3",
    "better-sqlite3":"^9.4.3",
    "cookie-parser": "^1.4.6",
    "cors":          "^2.8.5",
    "express":       "^4.18.3",
    "helmet":        "^7.1.0",
    "jsonwebtoken":  "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
